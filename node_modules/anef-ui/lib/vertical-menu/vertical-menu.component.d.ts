import { OnInit, OnChanges, SimpleChanges } from '@angular/core';
import { AbstractControl } from '@angular/forms';
import { MenuItem } from 'primeng/api';
export interface VMenuItem extends MenuItem {
    items?: MenuItem[] | MenuItem[][] | VMenuItem[];
    control?: AbstractControl;
}
export declare class VerticalMenuComponent implements OnInit, OnChanges {
    items: VMenuItem[];
    style: any;
    styleClass: string;
    multiple: boolean;
    topOffset: number;
    isWithMarkers: boolean;
    isSingleLevel: boolean;
    formattedItems: MenuItem[];
    targets: MenuItem[];
    handleScroll(): void;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onAnchorLink(event: any): void;
    private _getYOffset;
    private _parseItems;
    setMarkerStatus(item: MenuItem, control: AbstractControl): void;
    private addClass;
    private removeClass;
}
