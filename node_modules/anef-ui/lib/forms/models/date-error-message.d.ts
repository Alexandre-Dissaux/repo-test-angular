export interface DateErrorMessage {
    day: ErrorMessage;
    month: ErrorMessage;
    year: ErrorMessage;
    invalid: string;
    required?: string;
}
interface ErrorMessage {
    required: string;
    pattern?: string;
    length?: string;
    minMax?: string;
}
export {};
