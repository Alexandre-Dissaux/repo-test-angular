import { EventEmitter, OnChanges, OnInit, SimpleChanges, TemplateRef } from '@angular/core';
import { FormArray, FormBuilder, FormGroup, FormGroupDirective } from '@angular/forms';
export declare class RepeatFormComponent implements OnInit, OnChanges {
    private parentForm;
    private formBuilder;
    template: TemplateRef<any>;
    readOnly: boolean;
    required: boolean;
    maxNumber: number;
    minNumber: number;
    inputNumberLabel: string;
    removeLabel: string;
    confirmLabel: string;
    errorMessages: {
        required: string;
        pattern: string;
        min: string;
        max: string;
    };
    controlName: string;
    header: string;
    isFormTouched: boolean;
    entreprisesSizeVide: boolean;
    addedForm: EventEmitter<FormGroup>;
    addedFormZero: EventEmitter<any>;
    numberRegexPattern: RegExp;
    form: FormArray;
    formSize: FormGroup;
    isMinZeroReadOnly: boolean;
    constructor(parentForm: FormGroupDirective, formBuilder: FormBuilder);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private initForm;
    private patchForm;
    private patchFormSizeVide;
    private buildSizeValidators;
    confirmSize(): void;
    private addElement;
    clearAllElements(): void;
    private resetFormIsMinSizeZero;
    isMaxSizeReached(): boolean;
    readonly size: number;
    isMinSizeZero(): boolean;
}
