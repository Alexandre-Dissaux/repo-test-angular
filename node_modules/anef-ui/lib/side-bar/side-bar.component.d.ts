import { EventEmitter } from '@angular/core';
export declare enum SideBarType {
    DOCUMENTS = "DOCUMENTS",
    APPOINTMENT = "APPOINTMENT",
    MESSAGESINTERN = "MESSAGESINTERN",
    MESSAGES = "MESSAGES",
    HISTORIQUE = "HISTORIQUE",
    LIBRARY = "LIBRARY"
}
export interface SideMenuItem {
    icon?: string;
    title?: string;
    module: SideBarType;
    content?: any;
}
export declare class SideBarComponent {
    sections: SideMenuItem[];
    activeIndex: number;
    activeIndexChange: EventEmitter<number>;
    SideBarType: typeof SideBarType;
    constructor();
    handleChange(event: any): void;
    getIconClass(section: SideBarType): "fa fa-fw fa-paperclip" | "fa fa-fw fa-calendar" | "fa fa-fw fa-comments-o" | "fa fa-fw fa-envelope" | "fa fa-fw fa-history" | "fa fa-fw fa-book";
}
